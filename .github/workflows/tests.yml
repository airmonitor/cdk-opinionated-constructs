name: tests

on:
  push:
    branches:
      - main

jobs:
  synth:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v1
      with:
        node-version: 18.x

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        npm install -g aws-cdk
        cd test
        python -m venv .venv
        source .venv/bin/activate
        pip install -r requirements.txt

    - name: Invoke CDK synth
      run: |
        cd test
        source .venv/bin/activate
        export CDK_DEFAULT_ACCOUNT=${{ env.CDK_DEFAULT_ACCOUNT }}
        export CDK_DEFAULT_REGION=${{ env.CDK_DEFAULT_REGION }}
        cdk synth
